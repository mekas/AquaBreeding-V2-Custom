import 'package:fish/theme.dart';
import 'package:flutter/material.dart';

class InventarisPakanPage extends StatelessWidget {
  const InventarisPakanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pakan'),
        centerTitle: true,
        backgroundColor: backgroundColor1,
      ),
      backgroundColor: backgroundColor1,
      body: Column(),
    );
  }
}
